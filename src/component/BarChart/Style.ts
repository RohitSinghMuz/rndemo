import { StyleSheet } from "react-native";
import Dimension from "../../utils/Dimension";

const styles = StyleSheet.create({
    container: {
      alignItems: "center",
      marginTop: Dimension.n(16),
    },
    chart: {
      borderRadius: Dimension.n(12),
    },
  });

  export default styles